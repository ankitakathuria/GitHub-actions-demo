name: Secrets Demo

on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Secret  
        run: echo "Hello ${{secrets.MY_NAME}}"

      - name: Print Company
        run: echo "I work at ${{secrets.COMPANY_NAME}}"