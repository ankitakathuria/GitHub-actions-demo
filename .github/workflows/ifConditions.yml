name: last-topics

on:
  push:
  pull_request:

jobs:
  first-job:
    runs-on: ubuntu-latest


    steps:
      - name: Build Files
        run: npm run build

      - name: Test
        run: npm run test

      - name: Deploy
        if: ${{github.event_name == 'push' && github.ref_name == 'main'}}
        run: echo "Deploying"



